/**
 * Fallback reserved email for signup blocks when `VITE_ADMIN_EMAIL` is unset.
 * Must match backend `ADMIN_EMAIL` (primary super admin in Neon).
 */
export const SUPER_ADMIN_CREDENTIAL_EMAIL = "adminlegacywallet@gmail.com";
